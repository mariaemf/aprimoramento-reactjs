import React from "react";
import ReactDOM from "react-dom/client"; /*integração do react c a Dom*/
import { App } from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
